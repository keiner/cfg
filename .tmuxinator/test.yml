name: TEST root: ~/work/test

## LAYOUTS
#even-horizontal
#even-vertical
#main-horizontal
#main-vertical
#tiled


windows:
  - TMUXINATOR:
      layout: main-vertical
      panes: 
      - cd && cd .tmuxinator && vim test.yml 
      - cd && cd .tmuxinator && tcf 
  - ALIAS:
      layout: main-vertical
      panes: 
      - cd && ölkj && gitcs 
      - cd && ölkj &&  ali 
  - PI:
      layout: tiled
      panes: 
      - ölkj && sshpi 
  - zsh :
      layout: tiled
      panes: 
      - ölkj && top
      - ölkj && allip
      - ölkj && sshpi
      - 
